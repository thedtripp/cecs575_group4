****** Always check to make sure you are on your working branch A.K.A local branch *****

$git status #will display if you are working on Main/Master or local branch 

to pull changes from main/master branch to your local branch keep in mind your current changes on your local branch might get overwritten if not save properly for instance push your changes 
$git git pull branch name or 
example: pulling master into local branch 
$git pull origin/master

joining two branches together make sure the branches don't have any conflicts
$git merge branch-name

adding your changes to your local branch on github

first step:
$git add .

second step: 
$git commit -m "descriptive text"

third step:
$git push 

fourth step verify the changes have been push by going to the github project online repository.

